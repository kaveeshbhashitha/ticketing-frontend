export interface User {
  userId: string;
  userEmail: string;
  firstName: string;
  lastName: string;
  password: string;
  userRole: string;
  dateRegistered: string;
  timeRegistered: string;
  profileImage?: string;
}
